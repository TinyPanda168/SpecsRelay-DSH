# 更新记录

简体中文 | [English](CHANGELOG.en.md)

## 2026-09-24

- **桌面升级兼容性修复**：保留 DSH Desktop 2.0.13 的原生网页配套源码补丁，覆盖 Electron 与隔离宿主注册、进程通信、菜单层级和构建导出。安装器在修改 profile 前检查原生能力文件，缺失时明确提示；插件不再把此情况泛称为“不支持的客户端”。普通插件更新不修改桌面应用，见[修复与升级说明](docs/desktop-native-repair.md)。
- **增强服务切换**：侧栏增加“关闭 / Jev / Laya”设置，保存后下次整理生效；Laya 填服务地址与可选模型名称，插件只调用已有服务，不安装或启动模型。界面设置优先于旧环境开关，凭据分别读取且不保存到配置。
- **Laya 接口适配**：支持 `/v1/systemone` 判断接口，单候选预算、独立阈值、完整原文回退与禁止重定向；不自动切换其他服务。已验证模拟 HTTP 调用、配置持久化和 UI 切换，未验证真实 Laya 模型质量。实现方式与上游版本已加入[借鉴清单](docs/external-reference-watchlist.md#laya-接口适配--2026-09-24)。
- **按内容组合整理方法**：在原整理模型的同一次生成中选择必要的澄清、需求与验收覆盖、修正撤回梳理和内部审查，保持现有按钮与输出格式。
- **减少无效追问**：优先询问前置决策，每轮最多三个必要问题；回答或修订后重新评估相关问题，清除已回答、撤回或不再适用的项。
- **记录借鉴方式**：直接复用 Superpowers 的五项审查清单并保留 MIT 许可；grill-me、Spec-Kit、OpenSpec 的相关方法独立编写；Trellis 继续作为跟踪参考。详见[实际采用记录](docs/external-reference-watchlist.md#实际采用记录--2026-09-24)。
- **验证范围**：13 项相关自动化测试通过，验证实际 Skill 加载、请求次数、修订材料和既有交互；已补充六组内容验收样例，真实模型的语义质量尚未验收。未引入框架依赖、额外路由服务或持久化需求版本管理。

## 2026-09-22

- 修正 DSH Desktop 左侧栏入口顺序：SpecsRelay 排在“导入会话”下方、“手机连接”和“设置”上方。
- GitHub 仓库迁移至 [TinyPandaGame/SpecsRelay-DSH](https://github.com/TinyPandaGame/SpecsRelay-DSH)，安装器默认来源、安装说明和发布素材同步使用新地址。

## 0.10.0 — 2026-09-21

### 新增

- **回到网页继续澄清**：“需要你确认”区域增加“复制问题，回网页讨论”和“重新获取并整理”。可以把待确认问题粘贴到左侧 DeepSeek 原对话，讨论完成后重新获取完整对话并整理；原有的直接填写回答方式保留。复制仅写入剪贴板，不会自动发送消息。
- **可选长对话增强**：整理模型先确定目标、已确认决策、约束、验收标准和待确认问题的查找重点，Jev 筛选原文证据，整理模型再生成 Specs。整个过程包含在一次“整理当前对话”中，没有单独的压缩步骤或压缩比例选项。
- **可选会话接续分流**：Jev 可为“接续会话”的辅助整理选择模型；选择失败时沿用原路线，不改变 Coding Agent 主模型。

### 调整与修复

- **仅支持 DSH Desktop by anywhere-labs**：安装器和界面入口聚焦这一客户端，移除 Pilot Harness、DataElement 和 myYangyunfan 的专用适配及子进程桥。
- **统一左右布局**：左侧保留 DeepSeek 网页，右侧保留 SpecsRelay；较窄窗口下右侧控件自动换行，不再切换为标签页。
- **修正增强模式顶部布局**：macOS 和 Windows 的插件弹层避开 32 像素标题栏，网页与右侧面板一起下移，顶部背景与工作区主题保持一致。兼容模式和扩展窗口不重复增加顶部间距。
- **改进网页启动错误提示**：启动失败时提示查看具体错误，避免统一显示为“不支持的客户端”。
- **保护本地配置**：增加环境文件、凭据文件和本地运行状态的 Git 忽略规则；同步更新中英文使用说明和仅面向 DSH Desktop 的发布素材。

### 配置与升级

- Jev 两项能力分别开启，默认均关闭，Key 可选；不配置 Jev 仍可使用完整的需求整理流程。长对话增强启用并配置 Key 后，达到 24,000 字符的对话自动进入证据筛选，短对话直接整理；规划或筛选失败时回退到完整对话。配置方法见 [Jev 配置指南](docs/jev-features.md#长对话增强)。
- 更新插件后重启 DSH Desktop。客户端须已提供可用的原生网页服务；仅安装此插件不会修改桌面应用本体。窗口模式菜单与原生网页的层级由客户端负责，见 [集成说明](docs/desktop-client-adapters.md)。

### 验证

- 71 项自动化测试通过，覆盖安装识别、面板布局条件、复制与重新获取、会话接续、Jev 证据筛选及异常回退；测试不调用真实付费模型，不能代替真实对话上的筛选质量评估。
- 已检查本次待提交文件和此前未推送提交，并与本机凭据比对，未发现私人 Key；扫描中唯一的疑似项是密钥拦截测试中的虚拟值。

代码变更：[ef2b00c](https://github.com/TinyPandaGame/SpecsRelay-DSH/commit/ef2b00ccebc2db87572eb9fb1cbef03e6203493e)。
